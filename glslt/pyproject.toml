[project]
name = "glslt"

[project.scripts]
glsltc = "glslt:main"

[build-system]
requires = ["maturin>=0.15,<0.16"]
build-backend = "maturin"

[tools.maturin]
bindings = "pyo3"
features = ["python"]
strip = true
