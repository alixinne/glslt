[project]
name = "glslt"

[project.scripts]
glsltc = "glslt:main"

[build-system]
requires = ["maturin>=1.5,<1.6"]
build-backend = "maturin"

[tools.maturin]
bindings = "pyo3"
features = ["python"]
strip = true
