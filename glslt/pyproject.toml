[project]
name = "glslt"

[project.scripts]
glsltc = "glslt:main"

[build-system]
requires = ["maturin>=1.7,<1.8"]
build-backend = "maturin"

[tools.maturin]
bindings = "pyo3"
features = ["python"]
strip = true
